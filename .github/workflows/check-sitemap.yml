name: Verify sitemap completeness

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  check-sitemap:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Check every page is listed in sitemap.xml
        run: python3 scripts/check_sitemap.py
